int global1, global2;

int vector[20];

int main(string param1, bool param2) {
  int i, local;
  int local1, local2;

  global1 = -5;
  vector[foo()] = global1;

  if(!true) {
    local1 = !foo;
  } else {
    local2 = foo;
  }

  for(i = 0: *i: i = -1) {
    local = true;
    continue;
  }

  while(*1) do {
    local = *1;
    break;
  }

  input local;

  output local;
  output 1;
  output true;
  output "aa";

  foo();
  foo(*1);
  foo(1, "aa", *1);

  local << 1;
  vector[access()] >> 43;

  return 0;
}
